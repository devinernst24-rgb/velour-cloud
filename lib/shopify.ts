// Shopify integration removed — cart is now pure React + Stripe Checkout.
// Type stubs kept for any lingering imports.

export type ShopifyVariantId = string;

export const SHOPIFY_CONFIGURED = false;
